# Development / test / lint dependencies
# Install alongside runtime dependencies:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt

anyio[trio]>=4.0
httpx>=0.23
pytest>=7.0
pytest-anyio>=0.0.0
pytest-asyncio>=0.24
pytest-cov>=4.0
pytest-xdist[psutil]>=3.0
ruff==0.15.9
